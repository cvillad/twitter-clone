<nav class="nav nav-left flex-column align-items-end h-100">
  <div class="d-flex flex-column justify-content-between h-100">
    <div class="d-flex flex-column nav-icons">
      <%= link_to tweets_path, class: "nav-link" do %>
        <i class="fab fa-twitter fa-2x mt-2"></i>
      <% end %>
      <%= link_to tweets_path, class: "nav-link link" do %>
        <i class="fas fa-home"></i> <strong>Home</strong>
      <% end %>
      <%= link_to "", class: "nav-link link" do %>
        <i class="far fa-bell mr-1"></i> <strong>Notifications</strong>
      <% end %>
      <%= link_to user_path(current_user.username), class: "nav-link link" do %>
        <i class="far fa-user mr-1"></i> <strong>Profile</strong>
      <% end %>
      <div class="nav-link p-1 mt-2 w-100 d-flex justify-content-center">
        <%= link_to "Tweet", tweets_path, class: "btn btn-tweet btn-sm btn-primary w-100 py-2 text-white font-weight-bold" %>
        <%= link_to tweets_path, class: "btn-icon-tweet" do %>
          <i class="fas fa-plus-circle fa-2x mt-2"></i>
        <% end %>
      </div>
    </div>
    <div class="dropdown d-flex justify-content-center">
      <button class="btn mb-4 btn-nav" type="button" id="profileMenu" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        <div class="d-flex">
          <%= image_tag "default", class: "mt-1", width: 30, height: 30 %>
          <div class="d-flex nav-pp ml-0 ml-lg-2 flex-column justify-content-start">
            <strong style="color: rgb(217, 217, 217); height: 20px"> <%= current_user.name %> </strong>
            <small class="text-left"> @<%= current_user.username %>  </small>
          </div>
        </div>
      </button>
      <div class="dropdown-menu" aria-labelledby="profileMenu">
        <%= link_to "Log out @#{current_user.username}", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
      </div>
    </div>
  </div>
</nav>